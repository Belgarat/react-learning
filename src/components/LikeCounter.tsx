//export const LikeCounter = ({likes} : {likes: number}) => {
export const LikeCounter = () => {
  return(
    <>
    {//likes && <span>{likes}</span>
    }
    <span>#Likes: {Math.floor(Math.random() * 101)}</span>
    </>
  )
}